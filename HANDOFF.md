# BRAIN-XAI 홈페이지 인수인계 문서

부경대학교 뇌 기반 설명가능 인공지능(BXAI) 연구실 홈페이지.
기존 Google Sites (`sites.google.com/view/brainxai`) 내용을 옮겨 새로 만든 것.

최초 작성: 2026-09-08
최종 개정: 2026-09-08 (2차 — 언어 전환 버그 수정, 한국어 번역 정비)

---

## 1. 한눈에 보기

- **파일**: `index.html` 하나. 약 97KB.
- **의존성**: 없음. 빌드 도구, 프레임워크, 패키지 매니저 전부 불필요.
- **외부 요청**: Google Fonts 하나뿐. 인터넷이 없으면 시스템 폰트로 대체되고 레이아웃은 유지된다.
- **서버**: 필요 없다. 정적 파일 하나다.

편집은 텍스트 에디터로 `index.html`을 직접 연다. VS Code 권장.

현재 규모: 논문 12편, 특허 6건, 과제 25건, `data-ko` 224곳.

---

## 2. 배포

### GitHub Pages (권장)

1. 저장소 루트에 `index.html`을 커밋한다. **파일명이 정확히 `index.html`이어야 한다.** 브라우저로 내려받으면 `index (1).html` 처럼 되는 경우가 있으니 확인할 것.
2. 저장소 루트에 빈 파일 `.nojekyll`을 같이 올린다. 지금은 없어도 되지만, 나중에 `_images/` 처럼 밑줄로 시작하는 폴더를 추가하면 Jekyll이 그 폴더를 통째로 무시한다. 미리 넣어두면 그 문제가 안 생긴다.
3. Settings → Pages → Source를 `Deploy from a branch`, 브랜치 `main`, 폴더 `/ (root)` → Save.
4. 1~2분 뒤 `https://<계정>.github.io/<저장소>/` 에서 열린다.

- 무료 계정은 저장소가 public이어야 한다.
- 저장소 이름을 `<계정>.github.io`로 만들면 하위 경로 없이 루트 주소로 뜬다.
- 해시 라우팅(`#people`)이라 서버 리라이트 설정이 필요 없다. SPA를 Pages에 올릴 때 흔한 404 문제가 이 파일에는 없다.
- `?lang=ko` 쿼리도 정적 호스팅에서 그대로 동작한다.
- **캐시**: Pages는 CDN을 거치므로 갱신이 반영되는 데 10분쯤 걸린다. 안 바뀐 것처럼 보이면 기다렸다가 Ctrl+Shift+R.

### 그 밖의 방법

| 방법 | 절차 | 주소 |
|---|---|---|
| Netlify Drop | netlify.com/drop 에 파일 드래그 | 임의 주소, 나중에 변경 가능 |
| 학교 웹 공간 | FTP로 `index.html` 업로드 | `pknu.ac.kr` 하위 |

**갱신 방법**: 파일을 고쳐서 같은 자리에 다시 올리면 끝.

Google Sites는 당장 지우지 말고, 새 주소가 안정될 때까지 링크만 걸어두는 것을 권한다.

---

## 3. 화면 구조

한 파일 안에 다섯 화면이 들어 있고, 주소 해시로 전환된다.

```
#home 또는 해시 없음 → 히어로 + 지표 + Notice + Research
#people              → 지도교수, 연구원, 졸업생
#publications        → 논문, 특허
#projects            → 연구 과제, 발표·기술이전·언론, 연구 환경
#contact             → 연락처
#notice / #research  → 홈으로 이동 후 해당 위치로 스크롤
```

HTML에서는 `<div class="view" id="view-people">` 형태로 감싸여 있다. 라우터가 하나만 남기고 나머지에 `hidden`을 건다.

**화면을 추가하려면**

1. `<div class="view" id="view-teaching" data-title="Teaching" data-title-ko="강의" hidden>` 로 블록을 만든다.
2. 스크립트 안 `var PAGES={...}` 에 `teaching:1` 을 추가한다.
3. `<nav class="top-nav">` 에 `<a href="#teaching" data-ko="강의">Teaching</a>` 를 넣는다.

세 군데가 전부다.

---

## 4. 한국어 / 영어

**규칙**: HTML 안의 텍스트는 영어 원문, 한국어는 `data-ko` 속성에 넣는다.

```html
<h3 data-ko="구성원 모집">We're looking for new members</h3>
```

### 동작 방식 (2차 개정에서 바뀜)

언어 전환 함수는 요소에 자식 태그가 있는지에 따라 다르게 동작한다.

- **자식 태그가 없는 요소**: `innerHTML`을 통째로 바꾼다. 예전과 같다.
- **자식 태그가 있는 요소**: **요소 자신의 텍스트만** 바꾸고 자식은 그대로 둔다.

두 번째 경우가 이번에 고친 부분이다. 예전에는 자식까지 통째로 덮어써서, 아래 같은 구조에서 한국어로 바꾸면 `<span class="meta">`가 사라졌다.

```html
<li data-ko="한글 과제명">English title<span class="meta">기관 · 기간</span></li>
```

이 버그로 한국어 모드에서 과제 25건의 지원기관·기간, 특허 4건의 등록번호, 공지의 하위 목록이 전부 보이지 않았다. 지금은 자식이 살아남는다.

**따라서 `<span class="meta">` 안의 내용을 번역하려면 그 span에 따로 `data-ko`를 단다.**

```html
<li data-ko="한글 과제명">English title<span class="meta" data-ko="한국연구재단 · 2019.09 – 2020.08">National Research Foundation of Korea · 2019.09 – 2020.08</span></li>
```

현재 meta span 36개 중 22개에 `data-ko`가 붙어 있다. 나머지 14개는 `4N Inc.`, `LINC 3.0`, `X-Corps`처럼 번역할 것이 없는 항목이다.

### 작성 규칙

- 링크가 섞인 문장은 텍스트 부분만 `<span data-ko="...">`로 감싼다. **속성 안에 태그를 넣지 말 것.**
- `data-ko` 값 안에 큰따옴표를 쓰면 깨진다. 필요하면 작은따옴표를 쓴다.
- input placeholder는 `data-ko-ph` 를 쓴다.
- 화면 제목은 view 요소의 `data-title` / `data-title-ko` 로 지정한다.
- 새 문단에 `data-ko`를 빠뜨리면 한국어 모드에서 그 부분만 영어로 남는다. 그것 자체는 오류를 내지 않으니 급하면 나중에 채워도 된다.

**언어 결정 순서**: 주소의 `?lang=` → 없으면 브라우저 언어(한국어면 한국어) → 그 외 영어. 언어를 바꾸면 주소에 `?lang=ko`가 붙어서, 그 링크를 공유하면 상대도 같은 언어로 열린다.

한국어일 때는 Noto Sans KR로 바뀌고 `word-break: keep-all`이 적용된다.

### 용어 표기

| 영어 | 채택한 한국어 | 비고 |
|---|---|---|
| reservoir computing | 리저버 컴퓨팅 | 1차 문서에서는 "축적 컴퓨팅"이었다. 국내 논문에서 더 흔한 표기로 통일했다. 연구실 표기가 다르면 `sed -i 's/리저버 컴퓨팅/축적 컴퓨팅/g' index.html` 로 되돌린다 |
| brain–machine interface | 뇌-기계 인터페이스 | 한글 쪽은 붙임표(-)로 통일. 영문은 en dash(–) |
| spiking neural network | 스파이킹 신경망 | |
| explainable AI | 설명 가능한 AI | |
| electroceuticals | 전자약 | |
| forecast band | 예측 밴드 | horizon과 구분하려고 나눴다 |
| horizon | 예측 구간 | |

논문 제목은 한국어 모드에서도 영어 원문 그대로 둔다(학술 인용 관례). 미국 특허 제목도 공식 명칭이 영어라 그대로 뒀다.

---

## 5. 내용 추가·수정 방법

### 논문

`view-publications` 안 `<ul class="pubs">`에 항목을 복사해서 넣는다.

```html
<li data-kind="journal"><div class="yr">2026</div><div>
  <p class="pub-t">논문 제목</p>
  <p class="pub-m">저자 · <em>학술지명</em> 권, 쪽 · <a href="https://doi.org/..." target="_blank" rel="noopener">doi</a></p></div></li>
```

- `data-kind`는 `journal` 또는 `conf`. 필터 버튼이 이 값으로 동작한다.
- 논문 제목에는 `data-ko`를 붙이지 않는다.
- 위쪽이 최신이다. 연도 순서는 수동으로 맞춘다.
- 논문 수가 바뀌면 두 곳을 고친다: 홈 지표의 `<b data-count="12">`, 그리고 검색 결과 없음 문구의 `전체 12편` / `all twelve`.

### 연구 과제

`view-projects` 안 `<ul class="plain" id="projlist">`.

```html
<li data-src="gov" data-ko="한글 과제명">English project title<span class="meta" data-ko="한글 기관명 · 2026.03 – 2027.02">English agency · 2026.03 – 2027.02</span></li>
```

- `data-src`는 `gov`(정부·공공), `univ`(교내), `ind`(산업체) 중 하나. 필터가 이 값을 쓴다.
- `class="x"`가 붙은 항목은 처음에 접혀 있다. 최신 7개만 펼쳐두고 나머지에 `x`를 붙이는 식이다.
- 과제 수가 바뀌면 두 곳을 고친다: 홈 지표의 `data-count="25"`, 그리고 스크립트의 `'전체 25개 보기'` / `'Show all 25 projects'` 문자열.
- meta span의 `data-ko`를 빠뜨리면 한국어 모드에서 그 줄의 기관명만 영어로 남는다. 사라지지는 않는다.

### 구성원

지도교수 영역은 네 부분이다.

| 블록 | 클래스 | 내용 |
|---|---|---|
| 이름·직함 | `.pi-head` | 이름(h3)과 소속 한 줄 |
| 연락처 | `.pi-reach` | 이메일·전화·연구실 3열. 화면이 좁으면 자동으로 접힌다 |
| 이력 | `.pi-line` | 연도 + 세로선 타임라인 |
| 그룹 제목 | `.grouph` | Members / Alumni |

- `.pi-line`의 `<dd>`에 `class="now"`를 붙이면 점이 청록색으로 채워진다. 진행 중인 직위 표시다. 빼면 빈 점(과거)이 된다.
- **지도교수 사진**을 넣으려면 `.pi-head` 안 주석을 지우고 이미지를 넣는다. 나머지는 CSS가 알아서 맞춘다.
  ```html
  <img class="pi-photo" src="pi.jpg" alt="">
  ```

연구원·졸업생 목록은 `<div class="people">` / `<div class="people alumni">`.

```html
<div class="person"><b data-ko="홍길동">Gil-Dong Hong</b><span data-ko="컴퓨터공학">Computer Engineering</span></div>
```

졸업하면 `class="person"`을 `class="person alum"`으로 바꾸고 Alumni 블록으로 옮긴다.

> **확인 필요**: 현재 구성원 한글 이름은 영문 로마자에서 역추정한 것이다. 실제 표기와 대조해서 고쳐야 한다.

### 특허

`view-publications` 아래쪽 `<ul class="plain">`. 국내 특허는 `data-ko`에 공식 한글 명칭을 넣는다.

---

## 6. 히어로의 두 데모

둘 다 **시뮬레이션이다. 실제 측정 데이터가 아니다.** 각 패널 하단에 그렇게 명시해 두었다. 이 문구는 지우지 말 것.

### 음성 디코더 (왼쪽)

EEG 8채널 → 시계열 모델이 멜 스펙트로그램 복원 → ASR이 텍스트 디코딩.

주요 상수 (스크립트 내 `hero: EEG -> mel spectrogram -> ASR text` 블록):

| 변수 | 현재 값 | 의미 |
|---|---|---|
| `SPEAK` | 170 | 단어 하나의 발화 길이(프레임). 키우면 느려진다 |
| `GAP` | 62 | 단어 사이 간격 |
| `BINS` | 80 | 멜 빈 개수 |
| `slip=Math.random()<.15` | 15% | 오인식 발생률 |

- 어휘는 `VOCAB` 객체. `lines`가 문장, `near`가 각 단어의 혼동 후보다. 혼동 후보는 음향적으로 가까운 쌍으로 짜야 의미가 있다(WATER/LATER, 물을/불을).
- 스펙트로그램 무늬는 단어 문자열에서 결정적으로 생성된다. 같은 단어는 항상 같은 무늬가 나온다.
- n-best 확률은 softmax 결과이고 합이 1이다.
- WER은 확정된 단어가 의도한 단어와 다른 비율. 점선 밑줄은 확률 0.5 미만으로 확정된 단어 표시로, 둘은 별개 지표다.

### 추세 예측 (오른쪽)

리저버 컴퓨팅 기반 시계열 예측 + 특징 기여도.

- `HZ` 배열이 1d/5d/20d 각각의 예측 길이와 밴드 폭을 정한다.
- 방향 적중률은 실제로 계산한다. 예측 시점의 방향을 저장해 두고 구간이 지나면 실현값과 대조한다. 그래서 100%가 나오지 않는다.
- 특징 네 가지(모멘텀, 변동성, 거래량 변화, 리저버 상태)는 가상값이다. 실제 모델 출력으로 교체할 수 있다.

> **주의**: 금융 관련 화면이므로 "투자 자문이 아니다"라는 문구를 반드시 유지해야 한다.

실제 실험 수치가 있다면 상수만 바꿔서 맞출 수 있다. 다만 실측값을 넣는 순간 "시뮬레이션" 문구를 "실측 예시"로 정확히 바꿔야 한다.

---

## 7. 그 밖의 동작

- **Research 탭**: 세 탭 각각에 캔버스 데모(스파이크 래스터 / 커서 제어 / 예측 곡선)가 있다. 보이는 탭만 애니메이션이 돌고, 화면 밖으로 나가면 멈춘다.
- **지표 카운트업**: 화면에 처음 들어올 때 한 번만 올라간다.
- **접근성**: OS의 "동작 줄이기" 설정이 켜져 있으면 모든 애니메이션이 정지 화면으로 대체된다. `REDUCE` 변수가 이를 처리한다.
- **탭 비활성화**: 다른 탭으로 가면 애니메이션이 멈춘다. 노트북 배터리를 위한 처리다.

---

## 8. 디자인 토큰

CSS 최상단 `:root`에 모여 있다. 색을 바꾸려면 여기만 고치면 전체에 반영된다.

```
--paper   #E7ECEF   배경
--ink     #0F1A24   본문 글자
--signal  #14655C   강조 1 (청록, 링크·그래프)
--spike   #8E6B0F   강조 2 (황토, 경고·예측)
--rule    #B9C5CE   테두리
```

폰트는 영문 Archivo(제목·UI) + Source Serif 4(본문), 한글 Noto Sans KR, 수치는 IBM Plex Mono.

`.hero-meta` 클래스는 지금 쓰이지 않는다(2차 개정에서 해당 줄을 뺐다). 나중에 히어로에 한 줄 더 넣을 일이 있으면 그대로 쓰면 된다.

`.contact-box`(회색 박스)는 이제 Contact 화면에서만 쓴다. People 화면에서는 `.pi-reach`로 바뀌었다.

---

## 9. 변경 이력

### 2차 개정 (2026-09-08)

**히어로**

- 버튼 아래의 `Principal investigator · Hoon-Hee Kim, Ph.D. · Nuri-gwan (A13), 1F` 줄 삭제. 같은 정보가 People / Contact 화면에 있다.
- 연구 주제 태그를 Quantum machine learning / Neuromorphic computing / Brain–machine interface / Nonlinear timeseries 네 개로 교체. (이전: Neuroscience / Reservoir computing / Brain–machine interface / Explainability)

**언어 전환 버그 수정** — 4절 참조. 한국어 모드에서 자식 태그가 통째로 사라지던 문제.

**People 화면 지도교수 영역 재구성**

- 이름을 22px에서 clamp(26–34px)로 키워 화면의 기준점이 되게 했다.
- 이력을 세로선 타임라인으로 바꿨다. 진행 중인 직위(2022–현재 조교수, 2021–현재 과학 자문)는 채워진 청록 점, 끝난 직위는 빈 점. 이전에는 "now"가 두 줄 있는데 구분이 안 됐다.
- 회색 `.contact-box`를 걷어내고 실선 위 3열 배치(`.pi-reach`)로. 같은 박스가 Contact 화면에도 있어 중복이었다.
- 구성원 격자에서 빈 칸이 진한 회색 덩어리로 보이던 것 수정. 연구원 7명·졸업생 5명이 3열에 안 떨어져서 생기던 문제다.
- 인라인 style 속성 4개를 클래스로 정리(`.pi-head`, `.pi-role`, `.pi-reach`, `.grouph`).
- 지도교수 사진 자리를 주석으로 마련해 뒀다. 5절 참조.
- 연도의 "now"에 `data-ko="… – 현재"` 추가.

**한국어 정비**

- 기관명 22곳 한글화: 과학기술정보통신부·경찰청, 중소기업기술정보진흥원·중소벤처기업부, 국방과학연구소, 한국보건산업진흥원, 경기도경제과학진흥원, 한국연구재단, 대한의용생체공학회, 부경대학교, 소프트웨어융합혁신원
- 라벨 한글화: registered → 국내 등록, published → 국내 공개, US application → 미국 출원, under review → 심사 중, Talk → 발표, Broadcast → 방송, Technical know-how transfer → 기술이전
- 데모 캔버스 캡션 6곳 번역(뇌파 8채널, 복원된 멜 스펙트로그램 · 80빈, 실측값과 예측값, 래스터 · 40개 유닛, 커서 제어 · 명중, 실측 대 예측 · 오차)
- `forecast band`와 `horizon`이 둘 다 "구간"으로 번역돼 구분이 안 되던 것 → 예측 밴드 / 예측 구간
- 네비게이션 `Contact` "오시는 길" → "연락처". 섹션 제목의 "오시는 길"은 유지
- reservoir computing 표기를 "리저버 컴퓨팅"으로 통일(12곳)
- 문장 다듬기: "얌전하지 않은 시계열 예측" → "얌전하지 않은 시스템을 예측하기", "…읽히도록." 미완결 문장 정리, "중소벤처기업부중소벤처기업부" 중복 제거, 푸터 한글 보강

---

## 10. 남은 일

우선순위 순.

1. **구성원 한글 이름 검증** — 로마자 역추정이라 실제 표기와 다를 수 있다.
2. **용어 두 개 확인** — "리저버 컴퓨팅"(연구실 표기 확인), "소프트웨어융합혁신원"(교내 공식 명칭 확인. 직역이다).
3. **Research Areas 이미지** — 원본 Google Sites의 이미지를 못 가져왔다. 원본 파일이 있으면 각 탭에 추가.
4. **지도교수 사진, 연구실 사진** — 현재 사진이 하나도 없다. 지도교수 사진은 자리가 준비되어 있다(5절).
5. **논문 목록 최신화** — 2022년 이후 논문이 반영되어 있지 않다.
6. **파비콘** — 없다. 헤더의 SVG 로고를 그대로 쓰면 된다.
7. **미국 특허 한글 병기** — 지금은 영어 제목만 나온다. 병기할지 결정 필요.

---

## 11. 알려진 한계

- **검색엔진**: 모든 내용이 한 주소(`/`)에 있어서, 구글이 "논문 페이지"를 따로 잡지 못한다. 검색 유입이 중요해지면 화면별로 파일을 나눠야 한다(`people.html`, `publications.html` 식). 구조는 그대로 두고 분리만 하면 되므로 어렵지 않다.
- **콘텐츠 관리**: 논문이 50편을 넘어가면 HTML을 직접 고치는 게 번거로워진다. 그때는 목록을 JSON 파일로 분리하는 편이 낫다. 이 경우에도 서버는 필요 없다.
- **문의 폼, 회원 전용 영역**: 지금 구조로는 불가능하다. 서버나 외부 서비스(Formspree 등)가 필요하다.

---

## 12. 문제가 생기면

- **화면이 하얗게 뜬다** → 브라우저 개발자도구(F12) Console 탭에 오류가 찍힌다. 대개 직전에 편집한 곳의 따옴표나 태그 짝이 안 맞는 경우다.
- **한국어 모드에서 일부만 영어** → 그 요소에 `data-ko`가 없는 것이다. 과제·특허 줄의 기관명 부분이면 `<span class="meta">`에 `data-ko`가 없는 것이다.
- **한국어 모드에서 뭔가 사라진다** → 2차 개정에서 고친 버그다. 언어 전환 함수를 다시 손댔다면 4절을 확인할 것.
- **메뉴를 눌러도 화면이 안 바뀐다** → `PAGES` 객체에 해당 id가 등록되어 있는지 확인.
- **데모가 안 움직인다** → OS 동작 줄이기 설정이 켜져 있는지 확인.
- **GitHub Pages에서 수정이 반영 안 된다** → CDN 캐시. 10분 기다렸다가 Ctrl+Shift+R.
